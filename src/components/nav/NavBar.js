import React, { Component } from 'react'

class NavBar extends Component {
    render () {
        return (
           <div>
               <button>Home</button>
           </div> 
        )
    }
}

export default NavBar